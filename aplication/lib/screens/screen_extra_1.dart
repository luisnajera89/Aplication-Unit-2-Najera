import 'package:flutter/material.dart';

class Extra1Screen extends StatelessWidget {
  const Extra1Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Extra1Screen'),
      ),
    );
  }
}
