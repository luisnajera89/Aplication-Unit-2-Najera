import 'package:flutter/material.dart';

class Extra2Screen extends StatelessWidget {
  const Extra2Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Extra2Screen'),
      ),
    );
  }
}
